# run this code to install the relevant packages

packages = c(
    "RMySQL", # to connect to the database
    "Matrix", # lightweight sparse matrices
    "reshape2" # easy way to reorganize data
)

install.packages(packages)
